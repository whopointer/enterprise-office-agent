# 模板语法

## 基本占位符

在Word模板中使用 `{{变量名}}` 作为占位符:

```
尊敬的{{姓名}}：
    您好！感谢您于{{日期}}参加{{活动名称}}...
```

## 数据格式

JSON格式:
```json
{
  "姓名": "张三",
  "日期": "2026-06-08",
  "活动名称": "技术沙龙"
}
```

CSV格式:
```csv
姓名,日期,活动名称
张三,2026-06-08,技术沙龙
```

## 邮件合并

批量合并时，每行数据生成一个文档，所有 `{{列名}}` 占位符会被对应列的值替换。
